fastapi==0.141.1
# Explicit starlette pin overrides fastapi's transitive `>=0.46.0` —
# 0.46.x had CVE-2025-54121 (multipart DoS) and 0.48.x had
# CVE-2025-62727 (header parsing). 0.49.1 is the first version that
# clears both. fastapi 0.136.1 dropped the upper bound on starlette
# (`>=0.46.0` only), so the pin is compatible.
starlette==0.52.1
uvicorn[standard]==0.52.3
neo4j==6.2.0
graphdatascience==1.22
loguru==0.7.3
pydantic==2.13.4
pydantic-settings==2.15.0
httpx==0.28.1
prometheus-client==0.26.0
rapidfuzz==3.14.5
