apiVersion: v1
kind: Service
metadata:
  namespace: {{ .Release.Namespace }}
  name: fontem-consolidator
  labels:
    app: fontem-consolidator
spec:
  type: ClusterIP
  selector:
    app: fontem-consolidator
  ports:
    - name: http
      port: 8000
      targetPort: 8000
